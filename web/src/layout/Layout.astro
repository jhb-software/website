---
import type { PageMetaData } from '../cms/getPageData'
import '../styles.css'
import Header from './Header.astro'
import SEOMetadata from './SEOMetadata.astro'

type Props = PageMetaData & {
  lang: string
}

const { meta, lang } = Astro.props
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <SEOMetadata {...meta} />
  </head>
  <body class="prose">
    <Header />
    <slot />
  </body>
</html>
