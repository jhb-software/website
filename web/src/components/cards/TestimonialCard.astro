---
import type { Testimonial } from 'cms/src/payload-types'
import Img from '../Img.astro'

export type Props = Testimonial

const { project, author, text } = Astro.props

const customer =
  typeof project === 'object' && 'customer' in project && typeof project.customer === 'object'
    ? project.customer
    : undefined

if (!customer) {
  throw new Error(`Customer not populated, got: ${JSON.stringify(Astro.props)}`)
}
---

<figure class="border-border flex flex-col gap-5 border-t bg-transparent px-1 py-6">
  <header class="flex items-center gap-4">
    {
      typeof author.image === 'object' && (
        <Img
          image={author.image}
          size="sm"
          class="aspect-square h-10 w-10 shrink-0 rounded-full object-cover"
        />
      )
    }

    <div class="min-w-0 flex-1">
      <div class="text-primary truncate font-sans text-sm font-medium">
        {author.name}
      </div>
      {
        customer.websiteUrl ? (
          <a
            href={customer.websiteUrl}
            target="_blank"
            rel="noopener noreferrer"
            class="text-neutral-subtle ease-standard tracking-eyebrow hover:text-tertiary inline-block truncate font-mono text-xs uppercase transition-colors duration-150"
          >
            {customer.name}
          </a>
        ) : (
          <span class="text-neutral-subtle tracking-eyebrow inline-block truncate font-mono text-xs uppercase">
            {customer.name}
          </span>
        )
      }
    </div>
  </header>

  <blockquote class="text-neutral max-w-prose font-sans text-base leading-[1.55]">
    {text}
  </blockquote>
</figure>
